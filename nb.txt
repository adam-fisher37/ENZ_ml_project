# boilerplate
import numpy as np
import gendata_char as gen #current cores w/ true = 6
import datetime
import h5py

ang = np.array([55.,60,65])
setlen # want 7k total

# note: gendata on alpha has # in comments in txt file, make sure that is still true
# note: may want to use 'os' and 'fnmatch' to resolve file issue, or do tempfiles

dset = 'data'
com = 'ang: 55-5-65 deg, dset = data, run at: date'
gen.gen_dat(ang,setlen,com,dset)